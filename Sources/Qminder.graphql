query locationDetails($id: ID!) {
	location(id: $id) {
		lines {
			id
			name
		}
		desks {
			id
			name
		}
		users {
			id
			firstName
			picture
		}
	}
}

query userDetails($id: ID!) {
  user(id: $id) {
    id
    firstName
    lastName
    picture
  }
}
